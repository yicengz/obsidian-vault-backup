# 2026-07-11

## 起居（routine）模块

### 1. sync 脚本升级：接入 obsidian `footprint/`
- 把 obsidian `yiceng/footprint/` 作为起居的内容源，同步到 `content/routines/`
- 支持 obsidian transclusion 语法 `![[日记#heading]]` / `![[日记#^blockid]]`
- 同步时抽取被引用的片段，**原地内联**替换 `![[...]]`
- 日记原文不进 git，只有被引用的片段进入网站源码（隐私边界）
- 建议用 node 脚本重写 `scripts/sync-from-obsidian.sh`，复用 `gray-matter`

### 2. Timeline hover 浮窗预览
- `RoutineTimeline` 每一项加 antd `Popover`（hover 触发）
- 浮窗内容：`summary` + 正文预览（截断到 ~200 字，末尾 `…`）+ "点击查看详情"
- 正文预览在 **build 时截断**，`getAllRoutinesMeta()` 新增 `preview` 字段，避免完整 content 进 list bundle
- 点击整卡片仍跳 `/routine/[slug]` 详情页

### 落地顺序
1. 先做 sync 脚本（不改前端也能让 footprint 上网）
2. 再做 hover 浮窗
